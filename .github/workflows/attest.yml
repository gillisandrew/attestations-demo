name: Test whodunit
on:
  workflow_call:
    inputs:
      digest:
        type: string
        required: true
  
jobs:
  attest:
    runs-on: ubuntu-latest
    steps:
      - name: Attest
        uses: actions/attest-build-provenance@v1
        with:
          subject-digest: ${{ inputs.digest }}
    
